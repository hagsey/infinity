<div class="pagination">
  {{#if prev}}
    <a class="pagination-link" href="{{page_url prev}}"><i class="fa fa-angle-double-left"></i> Previous page</a> 
  {{/if}}

  {{#if next}}
    <a class="pagination-link" href="{{page_url next}}">Next page <i class="fa fa-angle-double-right"></i></a>
  {{/if}}
</div>
